# Docker Image Configuration
TRAEFIK_IMAGE=traefik:v3.2
CONTAINER_NAME=traefik

# Docker Socket Path
# For Docker: /var/run/docker.sock (default)
# For Podman: /var/run/podman/podman.sock
DOCKER_SOCKET=/var/run/docker.sock

# Network Configuration
NETWORK_NAME=traefik_default

# Port Configuration
HTTP_PORT=80
HTTPS_PORT=443

# Optional: Portainer Edge Agent Tunnel Port
# Uncomment in docker-compose.yml to enable
# EDGE_TUNNEL_PORT=8000

# ACME / Let's Encrypt Configuration
ACME_EMAIL=your-email@example.com
CERT_RESOLVER_NAME=myresolver
CERT_VOLUME_NAME=traefik_traefik-certs

# ACME Challenge Type:
# - HTTP Challenge (default): Works with any HTTPS port, requires port 80 open
# - TLS Challenge: Requires port 443 open (uncomment in docker-compose.yml)

# Dashboard Configuration
TRAEFIK_DASHBOARD=true
DASHBOARD_HOST=traefik.yourdomain.com

# Optional: Dashboard Basic Authentication
# Generate password hash with: htpasswd -nb admin yourpassword
# Example format: admin:$apr1$xxxxx (escape $ as $$ in docker-compose)
# DASHBOARD_AUTH_USERS=admin:$$apr1$$xxxxx

# Logging
LOG_LEVEL=INFO
